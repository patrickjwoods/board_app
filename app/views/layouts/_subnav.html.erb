<% if user_signed_in? %>
	<nav class="navbar navbar-secondary" role="navigation">
		<div class="container">  
		  	<ul class="nav navbar-nav">
			  	<li><%= link_to "All Projects", projects_path %></li>
			  	<li><%= link_to @project.name, [@project] unless @project.nil? %></li>
			  	<% if @board %>
				  	<li><%= link_to @board.title, [@project, @board] unless action_name == "new" %></li>
			  	<% end %>
		    </ul>
		    <ul class="nav navbar-nav new-project">
		    	<li>
		    		<%= link_to new_project_path do %>
							<span class="glyphicon glyphicon-plus"></span> New Project
						<% end %>
		    	</li>
			</ul>	
	  </div>
	</nav>
<% end %>